// POST - public route for authenticating username and password and generating a JWT token.import React from 'react'
import React from 'react'

function Authentication() {
    return (
      <div>
      Authentication here
      </div>
    );
  }
  export default Authentication